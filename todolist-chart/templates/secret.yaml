apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "{{ .Values.database.rootPassword }}"
  MYSQL_PASSWORD: "{{ .Values.database.appPassword }}"